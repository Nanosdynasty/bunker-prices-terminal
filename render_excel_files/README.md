Put non-sensitive `.xlsx` files here only if you want to test local-folder mode on Render.

Render runs on a cloud server, so it cannot read `C:\Users\...` folders from a tester's PC.
For real private workbooks, use the OneDrive cloud connector instead of committing files here.
